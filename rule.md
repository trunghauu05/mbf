1.	Khởi động: Mỗi khi bắt đầu cuộc trò chuyện và người dùng gõ rule, AI phải tự động đọc file rule.md này để nhớ lại các nguyên tắc làm việc.
2.	Sửa code: Tuyệt đối KHÔNG ĐƯỢC tự ý sửa code của bất kỳ dự án nào cho đến khi người dùng ra lệnh "tiến hành sửa code"
3.	Lên kế hoạch (Plan): CHỈ viết plan (kế hoạch) khi người dùng yêu cầu "viết plan". Các plan được viết ra phải hoàn toàn riêng biệt, KHÔNG ĐƯỢC viết đè lên plan cũ, trừ khi có lệnh "bổ sung plan".
4.	Giải thích code: Khi người dùng gửi code và yêu cầu giải thích, phải giải thích chi tiết từng dòng và từng cú pháp code (tại sao nó lại viết như vậy, có tác dụng gì và các sự nhờ vã từ các file khác và chúng tương tác với nhau thế nào, lưu ý là từng dòng. Ví dụ như tôi gửi đoạn code này: interface UseKhuVucReturn { items: IKhuVuc[]; total: number; loading: boolean; loadingMore: boolean; hasMore: boolean; fetchFirst: () => Promise<void>; loadMore: () => Promise<void>; reset: () => void; }. Thì phải giải thích tại sao lại khai váo interface ở đây và tạo hàm UseKhuVucReturn để làm gì items: IKhuVuc[]; sẽ lấy dữ liệu từ đầu và để làm gì. total: number; để làm gì và hoạt động thế noà vâng vâng.....)
5.	Hướng dẫn tính năng mới: Khi người dùng yêu cầu hướng dẫn làm một tính năng, phải hướng dẫn viết từng dòng code, kèm theo giải thích chi tiết cho mỗi dòng (viết như vậy có tác dụng gì). Phần giải thích phải nằm ngoài (bên dưới) đoạn code, và đoạn giải thích phải chi tiết như rule Giải thích code.
6.	Phong cách Code (Coding Style): Khi gợi ý hoặc viết code, LUÔN LUÔN bám sát theo phong cách, cấu trúc thư mục và cách tổ chức file của dự án mẫu.
